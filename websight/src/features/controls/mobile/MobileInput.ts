/**
 * Represents the input state in movement control for mobile devices.
 */
export class MobileInput {
    left = false;
    right = false;
    jump = false;
    interact = false;
    settings = false;
}
