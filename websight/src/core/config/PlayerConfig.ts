/**
 * Player attributes
 */
export const Attributes = {
    BOUNCE_AMOUNT: 0.25, // Player bounce factor (0 = no bounce, 1 = full elastic bounce)
    VELOCITY: 360, // How fast the player moves
    JUMP_HEIGHT: -800,
} as const;
