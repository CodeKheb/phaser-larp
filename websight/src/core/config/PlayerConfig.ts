/**
 * Player attributes
 */
export const Attributes = {
    BOUNCE_AMOUNT: 0.25, // How much the player bounces off the ground (1 would mean if he hits the ground, he will bounce back up from the same height)
    VELOCITY: 360, // How fast the player moves
    JUMP_HEIGHT: -800,
} as const;
